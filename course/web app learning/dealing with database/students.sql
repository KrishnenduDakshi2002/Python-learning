-- BULK INSERT students FROM students.csv WITH(FORMAT = 'CSV',FIRSTROW = 5);
-- -- SELECT * FROM student;
-- CREATE Table Student_details('Id' NOT NULL PRIMARY KEY ,'Name' TEXT NOT NULL ,'phone_num' NOT NULL  ,'email_id'  TEXT NOT NULL ,'address' TEXT
-- NOT NULL  ,'active' NOT NULL ,'college'  TEXT NOT NULL  );
SELECT * FROM Student_details;
